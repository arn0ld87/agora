# Istabgleich: Freigabekriterien 1.0.0 gegen Code, STATUS und Issues

> Recherche zu [#1652](https://github.com/arn0ld87/agora/issues/1652) (Teil von [#1651](https://github.com/arn0ld87/agora/issues/1651)).
> Stand: 26.09.2026, Baseline-Branch `feat/f006-sim-fidelity` @ `d80e1a764`.
> Methode: `code-review-graph` (semantic_search_nodes) für Contract-/Provider-Code, `gh` CLI für Issue-/PR-/CI-Status, direktes Lesen von `ROADMAP.md`, `docs/STATUS.md`, `docs/decisions/`, `.github/workflows/`.
> Die Checkboxen im 0.10-Block von `ROADMAP.md` sind veraltet und wurden **nicht** als Statusquelle verwendet — maßgeblich ist der tatsächliche Issue-Status und der Code.

Kriterien-Quelle: `ROADMAP.md` Abschnitt „1.0.0 — Stable Release > Freigabekriterien" (15 Punkte), inhaltlich deckungsgleich mit den fünf Kategorien in [#767](https://github.com/arn0ld87/agora/issues/767) (Verträge, Qualität, Betrieb, Produkt, Release).

## Tabelle

| # | Kriterium (ROADMAP 1.0.0 / #767) | Status | Beleg | Offenes Issue |
|---|---|---|---|---|
| 1 | Versionierte API-, Report-, Run- und Persistenzverträge | **Teilweise** | `schema_version` existiert je Vertrag: `backend/app/contracts/report_contract.py:950,1083,1343`, `report_v3.py:728` (Literal 4), `run_budget_contract.py:63,109,156`, `run_manifest_contract.py:47,117`, `pipeline_degradation_contract.py:119`. Kein `schema_version`-Feld in `project_contract.py`, `workspace_contract.py`, `persona_contract.py`, `run_record_contract.py`, `document_manifest_contract.py`. Kein globales API-Versionsschema (`docs/api.md` „Konventionen": kein `/api/v1`-Prefix, keine API-Versionsangabe). | [#1657](https://github.com/arn0ld87/agora/issues/1657) „Verträge 1.0: Versionierung und Deprecation-Regeln festlegen" (offen) |
| 2 | Dokumentierte Kompatibilitäts- und Deprecation-Regeln | **Offen** | Einzelne ADRs behandeln Einzelfälle (`docs/decisions/0005-keine-v2-report-migration.md`, `0010-vue-v4-route-consolidation.md`), aber keine generische Deprecation-Policy mit Entfernungsterminen. `frontend/src/router/index.ts:143` markiert `/settings-classic` nur als „bleibt für einen Release-Zyklus" ohne festes Datum. | [#1657](https://github.com/arn0ld87/agora/issues/1657) |
| 3 | Migrationen mit Test-, Resume- und Rollback-Pfaden | **Teilweise/erfüllt** | Migrationsskripte unter `backend/scripts/migrate_*.py` (Profile, Projekte, Reports, Runs, Simulationen → Postgres; `migrate_reports_v1_to_v2.py`, `migrate_v2_full_report_to_v3.py`). Dedizierte Tests: `backend/tests/test_evidence_migration.py`, `backend/tests/services/test_embedding_migration.py`, `backend/tests/integration/test_postgres_{llm_profile,simulation,report,project,run}_migration.py`. Kein explizit dokumentierter Rollback-Nachweis auf Systemebene (siehe #766). | keine eigenständige Lücke — deckt sich mit #766 |
| 4 | Genau eine produktive Oberfläche | **Erfüllt** | [#760](https://github.com/arn0ld87/agora/issues/760) „Vue-v4 als einziges Produktfrontend konsolidieren" ist geschlossen (28.07.2026). Router leitet Legacy-Pfade um statt sie zu duplizieren: `frontend/src/router/index.ts:143` (`/settings-classic` → Redirect), `:149-160` („Legacy-Prozess-Routen" → Redirect auf `/v4/*`). Einziger verbliebener textueller Verweis auf `agora-2026` liegt in einem Test (`frontend/src/router/__tests__/index.spec.ts`), nicht im produktiven Router. | — |
| 5 | Genau eine kanonische Provider-/Secret-/Routing-Architektur | **Teilweise** | `backend/app/llm/providers/registry.py::detect_provider` ist die SSoT; `backend/app/llm/providers/base.py::detect_provider` delegiert explizit dorthin (`return _detect_provider_registry(base_url, model, mode="http")`), `LLMClient._detect_provider` (`backend/app/llm/client.py:598-636`) ebenfalls, mit explizitem Kommentar „keine Detection-Heuristik neben registry.py::detect_provider" für `codex_cli`/`claude_cli`. Dokumentierte SSoT-Ausnahme bleibt Embedding: `docs/agents/architecture-ssot.md` „Bekannte SSoT-Ausnahme: Embedding (#1417)" — einige Runtime-Consumer lesen noch `Config.EMBEDDING_*`/Env statt des `EmbeddingConfigurationStore`. | [#1417](https://github.com/arn0ld87/agora/issues/1417) „Embedding: Runtime-SSoT fertigstellen" (offen) |
| 6 | Vollständig grüne erforderliche Backend-/Frontend-/Schema-/Security-/E2E-Gates | **Offen** | Aktueller `main`-Head (`28382450`, CI-Run [36170620784](https://github.com/arn0ld87/agora/actions/runs/36170620784)): Workflow `CI` ist rot, Job „Integration tests (echtes Neo4j/Redis/PostgreSQL)" schlägt fehl. `e2e-smokes`, `contract-gates`, `version-drift`, `Scorecard` sind grün. `codeql.yml` und `cve-monitor.yml` sind auf den letzten drei Läufen grün. | keine — siehe „Lücken ohne Issue" |
| 7 | Keine offenen P0/P1-Release-Blocker | **Teilweise** | Kein offenes Issue mit Label `P0`. Ein offenes Issue mit Label `P1`: [#1297](https://github.com/arn0ld87/agora/issues/1297) (Epic Report-Pipeline). Die in `ROADMAP.md` (0.10.0-Block) benannten P0/P1-Punkte sind gemischt: `#1472` (Restart-Sicherheit) und `#1265` (Report-Parallelität) sind **geschlossen**; `#1417` (Embedding-SSoT) und `#1470` (Alias-/Koreferenz) sind **offen**. `#1236` (Twitter-Recommender) ist geschlossen (Commits auf diesem Branch, `a13fa6f52`/`d80e1a764`). | siehe Einzel-Issues oben |
| 8 | Keine ungeklärten Critical/High-Security-Findings ohne dokumentierte Ausnahme | **Erfüllt (soweit prüfbar)** | Kein offenes Issue mit Label `security`, keine offenen Issues mit „cve" im Titel/Body. `cve-monitor.yml`-Läufe der letzten Tage grün, `codeql.yml` grün. | — |
| 9 | Fresh-Install, Backup, Restore, Upgrade, Rollback nachgewiesen | **Offen** | [#766](https://github.com/arn0ld87/agora/issues/766) „ops(0.10): Backup, Restore, Upgrade und Release-Artefakte verifizieren" ist offen; Stand 24.09.2026 laut Issue-Body: Werkzeuge (`scripts/restore-drill.sh`, `backend/scripts/restore_verify.py`, Runbook) existieren, aber „der echte Fresh-Host-Drill wurde noch nicht durchgeführt". [#1633](https://github.com/arn0ld87/agora/issues/1633) (offen) beschreibt einen konkreten Bug im Backup-Schritt: `restore-drill.sh --phase backup` scheitert am Neo4j-Dump, weil `/backups` im Container nicht gemountet ist. | [#766](https://github.com/arn0ld87/agora/issues/766), [#1633](https://github.com/arn0ld87/agora/issues/1633) |
| 10 | Release-Artefakte, Checksummen und SBOM vorhanden | **Teilweise** | SBOM wird erzeugt: `.github/workflows/docker-image.yml:284-296` (`anchore/sbom-action` → Artefakt `agora-image-sbom-spdx`) und `:586-598` (GHCR-SBOM). Kein Checksum-Schritt (`sha256sum`/`checksum`) im Workflow gefunden. #766 listet „Release-Artefakt-Checksummen + SBOM vollständig nachweisen" explizit als offenen Punkt. | [#766](https://github.com/arn0ld87/agora/issues/766) |
| 11 | Mindestens ein öffentlich nachvollziehbarer, reproduzierbarer Referenzlauf | **Offen** | [#765](https://github.com/arn0ld87/agora/issues/765) „research(0.10): Kalibrierungs- und Baseline-Suite für Produktnutzen" ist offen. | [#765](https://github.com/arn0ld87/agora/issues/765) |
| 12 | Dokumentierter Vergleich mit einer einfacheren LLM-Baseline | **Offen** | Gleicher Scope wie #765 (Vergleich Single-Prompt-Baseline / statische Persona-Liste / voller Agora-Lauf), Issue offen. | [#765](https://github.com/arn0ld87/agora/issues/765) |
| 13 | Sichtbare Grenzen und Non-Goals | **Erfüllt** | `docs/STATUS.md` Abschnitt „Was 0.9.x noch nicht behauptet"; ROADMAP-weite Regel „Kein Multi-User-/SaaS-/Kubernetes-Ausbau vor 1.0" mit Verweis auf ADR-0019; #767 nennt „Out-of-Scope" explizit (Multi-User/Teams, SaaS, Kubernetes/Helm, Federation, Plugin-System, Frontend-Rewrite). | — |
| 14 | Feature-Freeze seit `0.10-RC` eingehalten | **Offen/N.A.** | Kein `v0.10.0`-Tag existiert (`git tag -l "v0.10*"` liefert nichts; letzter Tag `v0.9.6`). Laut `ROADMAP.md:146` beginnt der Feature-Freeze erst „mit dem ersten 0.10.0-Release-Candidate" — dieser RC existiert noch nicht, das Kriterium ist damit noch nicht anwendbar. | — (kein RC, kein separates Issue nötig) |
| 15 | Finaler RC mindestens sieben Tage ohne neuen P0/P1-Blocker | **Offen/N.A.** | Folgt aus #14: ohne existierenden RC ist dieses Kriterium noch nicht erreichbar. | — |

## Lücken ohne Issue

- **Roter CI-Integrationstest-Job auf `main`**: Workflow `CI`, Job „Integration tests (echtes Neo4j/Redis/PostgreSQL)" ist auf dem aktuellen `main`-Head (`28382450`, Run [36170620784](https://github.com/arn0ld87/agora/actions/runs/36170620784)) rot. Widerspricht direkt Kriterium 6 („vollständig grüne erforderliche Gates"). Keine Suche nach „Integration tests" im Titel offener Issues liefert einen Treffer.
- **Fehlender Checksum-Nachweis für Release-Artefakte im CI**: SBOM wird erzeugt, aber kein `sha256sum`/Checksum-Schritt in `.github/workflows/docker-image.yml`. Liegt inhaltlich unter #766, dort aber nicht als separater, technisch benannter Teilschritt (Workflow-Datei, Zeile) aufgeführt.
- **Fehlendes globales API-Versionsschema**: `docs/api.md` dokumentiert keinen versionierten Pfad-Präfix (`/api/v1/…`) und keine Fünf-der-fünf-Verträge-Abdeckung für `schema_version` (Projekt-, Workspace-, Persona-, Run-Record- und Dokument-Manifest-Contracts haben kein Versionsfeld). Fällt inhaltlich unter #1657, dort aber ohne die konkrete Liste der noch unversionierten Contracts.

## Quellen

- `ROADMAP.md` (Abschnitte „1.0.0 — Stable Release", „0.10.0 — Release Candidate")
- `docs/STATUS.md` (Kurzurteil, Versionsstatus, Tests und Verifikation)
- `docs/api.md` (Konventionen)
- `docs/agents/architecture-ssot.md` (SSoT-Tabelle, Embedding-Ausnahme #1417)
- `docs/decisions/` (0005, 0010, 0019 gesichtet)
- Code: `backend/app/contracts/{report_contract,report_v3,run_budget_contract,run_manifest_contract,pipeline_degradation_contract,project_contract,workspace_contract,persona_contract,run_record_contract,document_manifest_contract}.py`; `backend/app/llm/providers/{registry,base}.py`; `backend/app/llm/client.py:598-636`; `frontend/src/router/index.ts`; `.github/workflows/{docker-image,codeql,cve-monitor,ci}.yml`
- GitHub Issues/PRs: #760, #765, #766, #767, #1236, #1265, #1274, #1297, #1323, #1345, #1400, #1417, #1470, #1472, #1633, #1651, #1652, #1657
- CI-Run [36170620784](https://github.com/arn0ld87/agora/actions/runs/36170620784) (`gh run view`)
- `code-review-graph` MCP (`semantic_search_nodes_tool`) für Contract- und Provider-Code-Exploration
