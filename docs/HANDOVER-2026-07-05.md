# Übergabe — Session 2026-07-05 (Security-/Dependency-Sprint + LLM-Refactor)

Kontext-Dokument für ein nachfolgendes LLM. Repo: `/Volumes/T7/Projekte/agora` (Backend: Python/uv, Frontend: Bun, CI: GitHub Actions).

## Was in dieser Session gemacht wurde

### 1. CVE-/Dependency-Hygiene (Kern der Session)
- **pip-audit-Baseline abgebaut**: 6 Pakete gebumpt, Audits (Backend + Frontend) laufen grün bis auf bewusst ignorierte Vulns.
- **Bewusste Ignores** (alle in `docs/dependency-risk-register.md` dokumentiert, mit **Hardstop 2026-07-30**):
  - `CVE-2026-1839` + `PYSEC-2025-217` (transformers 4.57.6): **kein Upstream-Fix released**, Fix-Target ≥4.58.0 existiert noch nicht.
  - `PYSEC-2026-139`, `PYSEC-2026-597` u. a. — siehe Register für Begründungen.
- **Wichtig zur Pin-Kette**: Blocker für den transformers-Bump ist **nicht mehr** sentence-transformers upstream (5.6.0 erlaubt `transformers>=4.41,<6`), sondern **unser eigener Pin** `sentence-transformers>=4.1.0,<5` (wegen camel-oasis 0.2.5). Sobald transformers 4.58 erscheint, geht der Bump vermutlich ohne Pin-Änderung.
- **CVE-Monitor-Workflow** (`.github/workflows/cve-monitor.yml`): wöchentlicher pip-audit-Lauf (Montag 06:00 UTC), schlägt fehl wenn ignorierte CVEs einen Fix bekommen oder der Hardstop reißt.
- **Issues #124 und #624**: Status geprüft (kein Fix verfügbar), Begründung im Kommentar aktualisiert, **beide geschlossen** — Tracking läuft weiter über Risk-Register + CVE-Monitor.
- **Frontend**: bun update (undici, vitest/coverage-v8, opentelemetry-Pakete etc.), Audit sauber.
- **Backend**: opentelemetry/core-Bumps, uv.lock aktualisiert, Overrides in `pyproject.toml`.

### 2. LLM-Client-Refactor (#582 — erledigt)
- `app/llm/llm_client.py` (vorher ~1635 LOC Monolith) **in Provider-Module gesplittet**: `app/llm/providers/base.py` + Provider-Dateien, `detect_provider()`-Logik extrahiert.
- `llm_client.py` ist jetzt **saubere Facade (~86 LOC, reine Re-Exports)** — API-kompatibel, Imports unverändert nutzbar.
- `LLM_DISABLE_JSON_OBJECT_MODE` ist **deprecated** (neuer, präziserer Name: `LLM_DISABLE_JSON_OBJECT_MODE` → siehe DeprecationWarning in `app/llm/json_mode.py`); Legacy-Tests angepasst, Token-Counter/Metrics-Tests grün.
- Ollama-Detection unterscheidet jetzt Cloud vs. lokal (base_url-Heuristik).

### 3. CI/Workflows
- **`docker-image.yml`** (Commit `8e85b25`): Docker-Build + Smoke + Publish laufen **nur noch auf `release/**`, `rc/**`, Tags und manuellem Dispatch** — nicht mehr auf jedem main-Push (Prod-Proxy-Smoke dauerte ~30 min). Vor einem Release: Branch pushen oder `gh workflow run docker-image.yml`.
- **#359 (Trivy-Container-Scan nach Audit-Window enforcen)**: erledigt, CVE-Gating aktiv.
- pytest: silent-exception-Tests gefixt (BLE001/ruff), Backend-Tests + Lint grün.

### 4. Code-Review-Graph (CRG)
- Graph neu gebaut, Embeddings regeneriert (Modell `text-embedding-3-small`), semantische Suche aktiv (~7768 Nodes).

## Was noch offen ist
| Prio | Item | Status |
|---|---|---|
| 1 | **#591** — Provider-Detection vereinheitlichen (Rest der Refactor-Kette #582→#590→#591) | offen |
| 2 | **#590** — Folge-Refactor aus der Kette | offen |
| 3 | **transformers ≥4.58 Bump** sobald released (behebt CVE-2026-1839 + PYSEC-2025-217), dann Ignores aus Audit-Configs + Risk-Register entfernen | wartet auf Upstream; **Hardstop 2026-07-30** |
| 4 | camel-oasis-Abhängigkeit prüfen, falls der `<5`-Pin auf sentence-transformers doch stört | nur bei Bedarf |

## Wichtige Dateien / Anlaufstellen
- `docs/dependency-risk-register.md` — Single Source of Truth für alle CVE-Ignores + Deadlines
- `.github/workflows/cve-monitor.yml` — wöchentlicher Audit
- `.github/workflows/docker-image.yml` — Release-only Smoke/Publish
- `app/llm/providers/` + `app/llm/llm_client.py` (Facade)
- Backend-Audit lokal: `cd backend && uv run pip-audit --strict` (Ignore-Flags siehe CI-Workflow)

## Konventionen dieser Codebase (Kurzfassung)
- Deutsch in Issues/Commits üblich, Commit-Stil: `type: beschreibung` (z. B. `security: …`, `refactor: …`).
- Dependency-Änderungen immer mit Audit + Tests + Register-Update in einem Commit.
- Keine CVE-Ignores ohne Eintrag im Risk-Register inkl. Hardstop-Datum.
