### Fixed (Interview-Anker-Fehlbindung — 2026-08-14)

- **Simuliertes Zitat mit fachlich falsch gebundenem Anker wird jetzt erkannt:** `validate_quote_anchors` prüfte einen `seed_anchor` bisher nur auf reine Existenz im `evidence_index` — ein real existierender `seed_doc:`-Anker, der auf einen Dokumentfakt (`source_kind=seed_corpus`) statt auf die zitierte Interviewantwort (`source_kind=agent_quote`) zeigt, bestand die Prüfung unbemerkt (Referenzlauf AURORA, 2026-08-14). Ein Anker mit falschem `source_kind` gilt jetzt wie ein unbekannter Anker als `unbound_evidence_refs` — sichtbar, ohne das Zitat hart zu verwerfen. Regressionstest: `backend/tests/services/test_seed_doc_anchor_binding.py::test_realer_anker_mit_falschem_source_kind_wird_als_ungebunden_gefuehrt`. Issue #1300.
