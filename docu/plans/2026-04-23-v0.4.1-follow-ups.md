# v0.4.1 Follow-ups Implementation Plan

> **For Claude/Codex:** Execute this plan incrementally with verification, documentation updates in `docu/`, and frequent commits/pushes after each safe block.

**Goal:** Finish the highest-value post-v0.4.1 engineering follow-ups without destabilizing the current working product.

**Architecture:** Work in small, verifiable blocks. Prefer low-risk structural improvements first: frontend lint-noise reduction, workspace-layout introduction, then further backend lint rollout. Keep existing routes and behavior stable; only refactor behind compatible interfaces.

**Tech Stack:** Vue 3 + Vite, Flask, Python 3.11, Ruff, pytest, npm, uv

---

## Current verified baseline

- Version: `0.4.1`
- `npm run check` passes
- Backend tests: `70/70`
- Frontend lint: `0 errors, 21 warnings`
- Working tree / remote: expected clean and synced before starting the next block

---

## Block A — Frontend lint-warning cleanup

### Task A1: Remove low-risk unused-variable warnings in step components

**Files:**
- Modify: `frontend/src/components/Step2EnvSetup.vue`
- Modify: `frontend/src/components/Step3Simulation.vue`
- Modify: `frontend/src/components/Step4Report.vue`
- Modify: `frontend/src/components/Step5Interaction.vue`
- Modify: `frontend/src/views/SimulationRunView.vue`
- Modify: `frontend/src/views/Process.vue`

**Steps:**
1. Remove or rename obviously unused locals/params (`e`, `_`, `emit`, `watch`, `link`) where behavior does not change.
2. Re-run frontend lint.
3. Stop once warnings meaningfully drop; do not mix in unrelated refactors.
4. Update `docu/p0-arbeitsprotokoll.md` with exact warning count delta.
5. Commit + push.

**Verification:**
```bash
cd frontend && npm run lint
cd frontend && npm run build
npm run check
```

---

## Block B — Workspace layout foundation

### Task B1: Introduce shared workspace shell primitives

**Files:**
- Create: `frontend/src/layouts/WorkspaceLayout.vue`
- Create: `frontend/src/layouts/WorkspaceHeader.vue`
- Create: `frontend/src/layouts/WorkspaceSplit.vue`
- Modify: `frontend/src/views/MainView.vue`
- Modify: `frontend/src/views/Process.vue`

**Steps:**
1. Extract common shell regions (header, left/right split, content wrapper) into reusable layout components.
2. Migrate one view at a time, keeping routes and emitted events stable.
3. Verify no visual/functional breakage via build + lint.
4. Update `docu/target-architecture.md` / `docu/p0-arbeitsprotokoll.md` if boundaries change materially.
5. Commit + push.

**Verification:**
```bash
cd frontend && npm run lint
cd frontend && npm run build
npm run check
```

---

## Block C — Backend lint rollout phase 2

### Task C1: Expand Ruff coverage to the next stable service cluster

**Candidate files:**
- `backend/app/services/simulation_manager.py`
- `backend/app/services/simulation_runner.py`
- `backend/app/services/graph_tools.py`
- `backend/app/storage/neo4j_storage.py`

**Steps:**
1. Run targeted Ruff on the candidate cluster.
2. Fix low-risk issues only (unused imports/locals, simple f-string cleanup, ambiguous variable names, safe exception narrowing).
3. Add cleaned files into `package.json` / `.github/workflows/ci.yml` scoped Ruff list.
4. Verify targeted tests for touched services.
5. Update `docu/p0-arbeitsprotokoll.md` with expanded lint coverage.
6. Commit + push.

**Verification:**
```bash
cd backend && uv run ruff check <touched files>
cd backend && uv run pytest <relevant tests>
npm run check
```

---

## Block D — Finalize real remaining open points

### Task D1: Recompute real open issues after the above blocks

**Files:**
- Modify: `docu/p0-arbeitsprotokoll.md`
- Modify: `CHANGELOG.md` (if release-worthy)
- Optional: `README.md` if user-facing behavior changed

**Steps:**
1. Recount backend tests and frontend warning count.
2. Re-evaluate whether Ruff should remain scoped or can move closer to repo-default.
3. Write a short “wirklich offen” list with only unresolved items.
4. Commit + push.

---

## Guardrails

- Do not regress current passing `npm run check`.
- Do not rewrite the app shell in one shot.
- Do not touch user-local scratch files unless asked.
- Keep `AGENTS.md` and `CLAUDE.md` in sync if technical facts change again.
